[project]
name = "workspace"
version = "0.1.0"
description = "Personal productivity workspace"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "pyyaml>=6.0.3",
    "docker>=7.0",
    "mcp>=1.0",
    "httpx>=0.27",
    "uvicorn>=0.30",
    "starlette>=0.37",
]
