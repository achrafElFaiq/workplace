[project]
name = "email-reader"
version = "1.0.0"
description = "Smart inbox reader — filters noise, surfaces what matters"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.3",
    "streamlit>=1.35.0",
]
